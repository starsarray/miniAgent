# 最小可用 Agent 需求文档

## 1. 项目目标

从零实现一个最小可用 Agent，支持多轮对话、session 状态维护、工具调用循环、跨轮次任务继续执行，并接入真实 LLM API。

## 2. 功能需求

### 2.1 多轮对话与 Session 维护

- 系统应支持同一 session 内的多轮用户输入与 Agent 回复。
- 系统应保存每个 session 的对话历史、任务状态、工具调用记录等上下文信息。
- 用户追问时，Agent 应能基于当前 session 的历史状态继续处理。

### 2.2 Agent Runtime

- 核心 Agent 主流程必须自行实现。
- 不得直接依赖 LangChain、OpenHands 等现成 Agent 框架完成主流程。
- 可以使用普通 SDK、HTTP 客户端、数据库、日志库等基础依赖。

### 2.3 Agent 执行循环

Agent 至少应支持以下循环：

1. 接收用户输入。
2. 结合 session 上下文判断下一步动作。
3. 判断是直接回复用户，还是调用工具。
4. 执行工具并获取工具结果。
5. 将工具结果加入上下文。
6. 继续推理下一步，直到输出最终答案或达到最大步数限制。

### 2.4 工具系统

系统至少应提供 3 个可调用工具。

必选或示例工具：

- `calculator`：执行基础数学计算。
- `search`：执行搜索，可使用 mock 数据。
- `read_docs` / `todo` / `weather`：任选或自定义一个工具。

工具应满足：

- 每个工具有明确名称、入参、返回值。
- Agent 能根据 LLM 输出选择并调用工具。
- 工具执行失败时应返回可处理的异常信息。

### 2.5 最大步数限制

- 每次 Agent 执行循环应设置最大步数。
- 达到最大步数后，系统应停止继续调用工具或推理。
- Agent 应向用户说明任务未继续执行的原因。

### 2.6 异常处理

系统应至少处理以下异常：

- LLM API 调用失败。
- 工具不存在。
- 工具入参格式错误。
- 工具执行失败。
- session 不存在或状态读取失败。

异常发生后，系统应给出清晰的错误信息，并避免程序崩溃。

### 2.7 工具调用 Trace / 执行日志

系统应记录 Agent 执行过程中的关键日志，包括：

- 用户输入。
- Agent 每一步动作。
- 工具名称。
- 工具入参。
- 工具返回结果。
- 异常信息。
- 最终回复。

日志可输出到控制台、文件或 session trace 中。

### 2.8 跨轮次继续执行

系统至少应支持一个跨轮次继续执行场景。

示例：

1. 第一轮用户发起任务。
2. Agent 创建任务并记录任务状态。
3. 第二轮用户追问任务进度。
4. Agent 从 session 或 memory 中读取已有任务状态。
5. Agent 基于已有状态继续处理，而不是将追问视为全新问题。

## 3. LLM API 要求

- 系统必须接入真实 LLM API。
- API Key 等敏感配置应通过环境变量或配置文件读取。
- 不应将 API Key 硬编码到代码中。

## 4. Memory 需求

- 系统应保存对话历史，用于多轮对话。
- 系统应保存任务状态，用于跨轮次继续执行。
- README 中应说明 memory 的召回时机与放置方式。

建议说明内容：

- 何时读取 memory：收到用户输入后、调用 LLM 前。
- 何时写入 memory：用户输入后、工具调用后、最终回复后、任务状态变化后。
- memory 放置位置：可使用内存、JSON 文件、数据库或其他持久化方式。

## 5. 非功能需求

- 实现应保持最小可用，不要求复杂 UI。
- 代码结构应清晰，便于阅读和运行。
- 系统应便于本地启动和验证。
- 不应引入不必要的复杂框架。

## 6. 交付内容

最终提交应包含：

1. Agent 源代码。
2. README，包含运行方式、系统设计、memory 的召回时机与放置方式说明。
3. AI Prompt 与问题解决记录。

## 7. 验收标准

- 可以启动 Agent 并输入用户问题。
- Agent 能完成至少一次直接回答。
- Agent 能完成至少一次工具调用并基于工具结果回复。
- Agent 至少包含 3 个工具。
- Agent 有最大步数限制。
- Agent 有基本异常处理。
- Agent 有工具调用 trace 或执行日志。
- Agent 支持多轮对话和 session 维护。
- Agent 支持至少一个跨轮次继续执行场景。
- Agent 使用真实 LLM API。
