# 最小可用 Agent 系统架构

## 1. 架构目标

系统采用最小可用架构，实现一个不依赖现成 Agent 框架的 Agent Runtime，支持多轮对话、session 维护、工具调用、执行日志和真实 LLM API 接入。

## 2. 总体架构

```text
用户输入
  |
  v
CLI / Web API
  |
  v
Agent Runtime
  |
  +-- Memory Store
  |
  +-- LLM Client
  |
  +-- Tool Registry
  |     |
  |     +-- calculator
  |     +-- search
  |     +-- todo
  |     +-- weather
  |
  +-- Trace Logger
  |
  v
最终回复
```

## 3. 核心模块

### 3.1 CLI / Web API

负责接收用户输入并返回 Agent 回复。

最小实现可使用命令行交互：

- 读取用户输入。
- 生成或传入 `session_id`。
- 调用 Agent Runtime。
- 输出最终答案。

### 3.2 Agent Runtime

Agent Runtime 是系统核心，负责控制完整执行循环。

主要职责：

- 读取 session 上下文。
- 组装 prompt。
- 调用 LLM 判断下一步动作。
- 解析 LLM 输出。
- 调用工具。
- 写入工具结果和对话历史。
- 判断是否继续执行。
- 控制最大步数。
- 返回最终答案。

### 3.3 LLM Client

负责与真实 LLM API 通信。

主要职责：

- 从环境变量读取 API Key。
- 发送 messages / prompt。
- 获取 LLM 返回结果。
- 处理 API 调用失败、超时等异常。

### 3.4 Tool Registry

负责维护可用工具列表，并按名称执行工具。

主要职责：

- 注册工具名称、描述、参数结构。
- 根据 Agent 动作查找工具。
- 执行工具并返回结果。
- 处理工具不存在、参数错误、执行失败等异常。

### 3.5 Tools

系统至少包含 3 个工具。

建议工具：

- `calculator`：执行基础数学计算。
- `search`：返回 mock 搜索结果。
- `todo`：创建任务、查询任务状态，用于跨轮次继续执行。
- `weather`：查询城市当前天气。

### 3.6 Memory Store

负责保存和读取 session 相关状态。

最小实现可使用内存对象或 JSON 文件。

保存内容：

- `session_id`
- 对话历史
- 任务状态
- 工具调用记录
- 最近一次最终回复

### 3.7 Trace Logger

负责记录 Agent 执行过程。

记录内容：

- 用户输入。
- 当前 step。
- LLM 决策结果。
- 工具名称。
- 工具入参。
- 工具返回值。
- 异常信息。
- 最终回复。

## 4. 执行流程

### 4.1 单轮执行流程

```text
1. 用户输入问题
2. Runtime 读取 session memory
3. Runtime 将 10 轮之前的旧记忆压缩到 summary
4. Runtime 组装 prompt：summary memory、最近 10 轮记忆、session state、工具列表
5. Runtime 调用 LLM
6. LLM 返回动作
7. 如果是 final，则直接回复用户
8. 如果是 tool_call，则调用工具
9. Runtime 记录工具结果
10. Runtime 继续调用 LLM
11. 由 LLM 判断是否继续调用工具或输出 final
12. 直到获得 final 或达到最大步数
```

### 4.2 跨轮次继续执行流程

```text
第一轮：
1. 用户：帮我创建一个待办任务
2. Agent 调用 todo 工具创建任务
3. Memory Store 保存任务状态
4. Agent 回复任务已创建

第二轮：
1. 用户：刚才那个任务现在怎么样了？
2. Agent 读取当前 session 的任务状态
3. Agent 调用 todo 工具查询或更新任务
4. Agent 基于已有状态回复用户
```

## 5. 数据结构设计

### 5.1 Session

```json
{
  "session_id": "default",
  "messages": [],
  "tasks": {},
  "trace": []
}
```

### 5.2 Message

```json
{
  "role": "user | assistant | tool",
  "content": "消息内容",
  "created_at": "2026-06-06T00:00:00Z"
}
```

### 5.3 Tool Call

```json
{
  "name": "calculator",
  "arguments": {
    "expression": "1 + 2"
  }
}
```

### 5.4 Tool Result

```json
{
  "tool": "calculator",
  "success": true,
  "result": "3",
  "error": null
}
```

### 5.5 Trace Item

```json
{
  "step": 1,
  "type": "tool_call",
  "input": {},
  "output": {},
  "error": null
}
```

## 6. LLM 输出协议

为了便于 Runtime 解析，LLM 应返回结构化 JSON。

直接回复：

```json
{
  "type": "final",
  "answer": "最终回复内容"
}
```

工具调用：

```json
{
  "type": "tool_call",
  "tool": "calculator",
  "arguments": {
    "expression": "1 + 2"
  }
}
```

## 7. 最大步数控制

每次用户输入触发一次 Agent 执行。

执行过程中维护 `step_count`：

- 每调用一次 LLM 或工具后递增。
- 当 `step_count >= max_steps` 时停止执行。
- 默认 `max_steps` 可设置为 5。

达到限制后返回：

```text
已达到最大执行步数，本次任务暂时停止。
```

## 8. 异常处理

系统应在 Runtime 层统一捕获异常。

处理方式：

- LLM API 失败：记录日志并返回 API 调用失败提示。
- JSON 解析失败：记录 LLM 原始输出并返回格式错误提示。
- 工具不存在：返回工具不存在错误。
- 工具参数错误：返回参数错误信息。
- 工具执行失败：返回工具失败信息。
- memory 读写失败：返回 session 状态错误信息。

## 9. 目录结构建议

```text
miniAgent/
  README.md
  需求文档.md
  系统架构.md
  src/
    main.py
    agent.py
    llm_client.py
    memory.py
    tool_registry.py
    tools/
      calculator.py
      search.py
      todo.py
  logs/
    trace.log
```

## 10. 设计约束

- 不使用 LangChain、OpenHands 等 Agent 框架实现主流程。
- 真实 LLM API 只通过 LLM Client 调用。
- API Key 不写入代码。
- 工具调用必须经过 Tool Registry。
- session 状态必须经过 Memory Store 读取和写入。
- Runtime 只负责流程控制，不直接实现具体工具逻辑。

## 11. 验收对应关系

- 多轮对话：由 Memory Store 保存 messages。
- session 维护：由 `session_id` 关联上下文。
- 摘要记忆：超过 10 轮时，用 LLM 将 `已有 summary + 刚掉出最近 10 轮的一轮旧记忆` 增量压缩到 `summary`，最多 200 字。
- 工具调用：由 Tool Registry 和 Tools 实现。
- 最大步数：由 Agent Runtime 的 `max_steps` 控制。
- 异常处理：由 Runtime、LLM Client、Tool Registry 分层处理。
- trace 日志：由 Trace Logger 记录。
- 跨轮次继续执行：由 todo 工具和 tasks 状态实现。
- 真实 LLM API：由 LLM Client 接入。
